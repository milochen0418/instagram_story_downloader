[tool.poetry]
name = "mistories"
version = "0.1.0"
description = "MiStories — back up your Instagram Stories, built with Reflex"
authors = []
readme = "README.md"

[tool.poetry.dependencies]
python = "~3.11"
reflex = "0.8.26"
yt-dlp = "*"
playwright = "^1.59.0"
requests = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
